#include <cstdio>
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>

#define max 101

using namespace std;

int amain()
{
	int t, n, v, u, capital;
	int p[max];
	cin >> t;

	while (t--)
	{
		cin >> n;
		for (int i = 1; i <= n; ++i)
		{
			cin >> p[i];
		}

		for (int j = 1; j < n; ++j)
		{
			cin >> v >> u;
		}
		
	}

	return 0;
}